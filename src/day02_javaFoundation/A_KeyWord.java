package day02_javaFoundation;

/**
 *  定义：被 Java 语言赋予了特殊含义，用做专门用途的字符串（或单词），全部关键字都是小写字母。
 *  说明：关键字一共50个，其中const和goto都是保留字(reserved word);
 *       true,false,null不在其中，它们看起来像关键字，其实是字面量，表示特殊的布尔值和空值。
 *  分类：
 *      定义数据类型：
 *          class    int  C     enum     byte      short     interface
 *          long     float     double   char      boolean   void
 *      定义流程控制：
 *          if       else      switch   case      default   while
 *          do       for       break    return    continue
 *      定义访问权限：
 *          public   private   protected
 *      类函数及变量：
 *          final    abstract  static   synchronized
 *      定义类间关系：
 *          extends  implements
 *      创建引用实例：
 *          new      this      super    instanceof
 *      用于异常处理：
 *          try      catch     finally  throw     throws
 *      定义包的使用：
 *          package  import
 *      其他修饰：
 *          native   strictfp  assert   volatile  transient
 *      保留字：
 *          const    goto
 *
 */
public class A_KeyWord {
    public static void main(String[] args) {
        System.out.println("this is key word description document.");
    }
}
